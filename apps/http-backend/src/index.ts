import express from "express";
import jwt from "jsonwebtoken"
const app = express();
app.post("/signup", () => {

})
app.post("/signin", () => {

})
app.post("/room", () => {

})

app.listen("3030", () => {
    console.log("server started");
})